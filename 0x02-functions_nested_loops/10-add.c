#include "main.h"
/**
 * add - adds two numbers together.
 * @i: the first number to be added
 * @x: the second number to be added
 * Return: the sum of two numbers.
 */
int add(int i, int x)
{
	return (i + x);
}

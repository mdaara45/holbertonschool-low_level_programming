#include "main.h"
/**
 * mul - multiply two integers together.
 * @a: the first to multiply
 * @b: the second to multiply
 * Return: the multiple of two numbers.
 */
int mul(int a, int b)
{
	return (a * b);
}
